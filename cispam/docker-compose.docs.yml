services:
  docs:
    image: cispam_local_docs
    container_name: cispam_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./cispam:/app/cispam:z
    ports:
      - '9000:9000'
    command: /start-docs
