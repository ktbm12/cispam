volumes:
  cispam_local_postgres_data: {}
  cispam_local_postgres_data_backups: {}
  cispam_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: cispam_local_django
    container_name: cispam_local_django
    depends_on:
      - postgres
      - redis
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: cispam_production_postgres
    container_name: cispam_local_postgres
    volumes:
      - cispam_local_postgres_data:/var/lib/postgresql/18/docker
      - cispam_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  redis:
    image: docker.io/redis:7.2
    container_name: cispam_local_redis
    volumes:
      - cispam_local_redis_data:/data

  celeryworker:
    <<: *django
    image: cispam_local_celeryworker
    container_name: cispam_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: cispam_local_celerybeat
    container_name: cispam_local_celerybeat
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: cispam_local_flower
    container_name: cispam_local_flower
    ports:
      - '5555:5555'
    command: /start-flower
